[project]
name = "iceberg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyiceberg[s3fs,pyarrow,nessie]>=0.7.0",
]

[tool.uv.workspace]
members = [
    "branch",
]
